# Values can be Docker Image name or Label (Ref.: https://docs.datadoghq.com/agent/autodiscovery/)
docker_images:
  - jmx

init_config:
  is_jmx: true
  collect_default_metrics: true

instances:
  - host: "%%host%%"
    port: "%%port_0%%"
